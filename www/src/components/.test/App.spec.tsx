import * as React from 'react';
import { shallow } from 'enzyme';

import App from '../App';

it('if rendered text matches', () => {
    //const text = (count: number, title: string) => `App: ${count} :${title}`
    //const title = "testABC";
    //const el = shallow(<App text={title} />);
    //for (var count = 1; count < 10; count++) {
    //    el.simulate('click');
    //    expect(el.text()).toEqual(text(count, title));
    //}
});